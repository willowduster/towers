<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200" fill="none">
  <!--
    PLACEHOLDER LOGO
    Replace this file with your actual band logo.
    Keep it as logo.svg (SVG is preferred for sharpness at any size).
    Your logo should have a black fill with a transparent background.
    The site CSS applies `filter: invert(1)` so the black logo renders
    as white on the dark background automatically.
  -->
  <!-- Simple tower silhouette placeholder -->
  <rect x="85"  y="10"  width="30" height="140" fill="#000"/>
  <rect x="50"  y="70"  width="100" height="30"  fill="#000"/>
  <rect x="55"  y="150" width="20"  height="40"  fill="#000"/>
  <rect x="125" y="150" width="20"  height="40"  fill="#000"/>
  <polygon points="85,10 100,0 115,10" fill="#000"/>
</svg>
